import{NextRequest,NextResponse}from"next/server";function ema(a:number[],n:number){const k=2/(n+1);let e=a[0];for(let i=1;i<a.length;i++)e=a[i]*k+e*(1-k);return e}function rsi(a:number[],n=14){if(a.length<n+1)return 50;let g=0,l=0;for(let i=a.length-n;i<a.length;i++){const d=a[i]-a[i-1];if(d>0)g+=d;else l-=d}return l===0?100:100-100/(1+(g/n)/(l/n))}function atr(c:any[],n=14){let z=0;for(let i=c.length-n;i<c.length;i++)z+=Math.max(c[i].h-c[i].l,Math.abs(c[i].h-c[i-1].c),Math.abs(c[i].l-c[i-1].c));return z/n}export async function POST(req:NextRequest){try{const{candles,news=[]}=await req.json();if(!Array.isArray(candles)||candles.length<40)throw Error("Insufficient market data");const closes=candles.map((x:any)=>+x.c),f=ema(closes,12),s=ema(closes,26),r=rsi(closes),a=atr(candles),last=closes.at(-1),prev=closes.at(-2);let score=0;const reasons:string[]=[];if(f>s){score+=2;reasons.push("EMA trend structure is bullish")}else{score-=2;reasons.push("EMA trend structure is bearish")}if(r>55&&r<75){score++;reasons.push("RSI supports bullish momentum")}else if(r<45&&r>25){score--;reasons.push("RSI supports bearish momentum")}else reasons.push("RSI is neutral or extended");if(last>prev){score++;reasons.push("Latest close is above the prior close")}else{score--;reasons.push("Latest close is below the prior close")}const text=news.map((n:any)=>n.title||"").join(" ").toLowerCase(),pos=(text.match(/surge|rally|gain|growth|bull|rise|strong|beat/g)||[]).length,neg=(text.match(/fall|drop|loss|bear|risk|crash|weak|cut/g)||[]).length;if(text&&pos>neg){score+=.5;reasons.push("Headline tone is mildly positive")}else if(text&&neg>pos){score-=.5;reasons.push("Headline tone is mildly negative")}else reasons.push("Headline tone is mixed or unavailable");let signal="WAIT";if(score>=3)signal="STRONG BUY";else if(score>=1.5)signal="BUY";else if(score<=-3)signal="STRONG SELL";else if(score<=-1.5)signal="SELL";const confidence=Math.round(Math.min(94,52+Math.abs(score)/4.5*40));if(signal==="WAIT")reasons.push("Factors are not aligned strongly enough, so Sentinel refuses to force a trade");return NextResponse.json({signal,confidence,price:last,emaFast:f,emaSlow:s,rsi:r,atr:a,reasons,updatedAt:new Date().toISOString()})}catch(e:any){return NextResponse.json({error:e.message||"analysis failed"},{status:400})}}