"use client";
import {useEffect,useMemo,useRef,useState} from "react";
type Candle={t:number;o:number;h:number;l:number;c:number;v:number}; type Result={symbol:string;name?:string;exchange?:string;type?:string};
const quick:Result[]=[{symbol:"BTCUSDT",name:"Bitcoin / USDT",exchange:"BINANCE",type:"CRYPTO"},{symbol:"ETHUSDT",name:"Ethereum / USDT",exchange:"BINANCE",type:"CRYPTO"},{symbol:"EURUSD=X",name:"EUR / USD",exchange:"GLOBAL",type:"FOREX"},{symbol:"GBPUSD=X",name:"GBP / USD",exchange:"GLOBAL",type:"FOREX"},{symbol:"GC=F",name:"Gold",exchange:"GLOBAL",type:"COMMODITY"},{symbol:"CL=F",name:"Crude Oil",exchange:"GLOBAL",type:"COMMODITY"},{symbol:"^GSPC",name:"S&P 500",exchange:"US",type:"INDEX"},{symbol:"^NDX",name:"Nasdaq 100",exchange:"US",type:"INDEX"}];
function fmt(n:number|undefined){return n==null||!Number.isFinite(n)?"—":n.toLocaleString(undefined,{maximumFractionDigits:8})}
function Chart({data}:{data:Candle[]}){const pts=useMemo(()=>{if(!data.length)return"";const lo=Math.min(...data.map(x=>x.l)),hi=Math.max(...data.map(x=>x.h)),span=Math.max(hi-lo,1e-9);return data.map((x,i)=>`${(i/(data.length-1))*900},${330-((x.c-lo)/span)*300}`).join(" ")},[data]);return <svg viewBox="0 0 900 340" preserveAspectRatio="none"><polyline fill="none" stroke="currentColor" strokeWidth="3" points={pts}/></svg>}
export default function Home(){
 const [symbol,setSymbol]=useState("BTCUSDT"),[query,setQuery]=useState("BTCUSDT"),[interval,setInterval]=useState("1h"),[data,setData]=useState<Candle[]>([]),[analysis,setAnalysis]=useState<any>(null),[news,setNews]=useState<any[]>([]),[provider,setProvider]=useState(""),[results,setResults]=useState<Result[]>([]),[loading,setLoading]=useState(false),[error,setError]=useState(""),[image,setImage]=useState(""),[streaming,setStreaming]=useState(false),fileRef=useRef<HTMLInputElement>(null),wsRef=useRef<WebSocket|null>(null);
 async function search(q:string){setQuery(q);if(q.trim().length<2){setResults([]);return}try{const r=await fetch("/api/search?q="+encodeURIComponent(q),{cache:"no-store"});const j=await r.json();setResults(j.results||[])}catch{setResults([])}}
 async function load(s:string,tf=interval){setLoading(true);setError("");setAnalysis(null);try{const r=await fetch("/api/market?symbol="+encodeURIComponent(s)+"&interval="+tf+"&limit=180",{cache:"no-store"});const j=await r.json();if(!r.ok)throw new Error(j.reason||"DATA UNAVAILABLE");setData(j.candles||[]);setProvider(j.provider||"");setSymbol(j.symbol||s);const a=await fetch("/api/analyze",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({candles:j.candles||[]})});const aj=await a.json();if(!a.ok)throw new Error(aj.error||"Analysis failed");setAnalysis(aj);const n=await fetch("/api/news?q="+encodeURIComponent(j.symbol||s),{cache:"no-store"});const nj=await n.json();setNews(nj.items||[])}catch(e:any){setData([]);setError("DATA UNAVAILABLE / NO TRADE — "+(e&&e.message?e.message:"verified provider unavailable"))}finally{setLoading(false)}}
 useEffect(()=>{void load("BTCUSDT")},[]);
 useEffect(()=>{wsRef.current?.close();setStreaming(false);if(!/^[A-Z0-9]{3,20}USDT$/.test(symbol))return;const ws=new WebSocket("wss://data-stream.binance.vision/ws/"+symbol.toLowerCase()+"@kline_"+interval);wsRef.current=ws;ws.onopen=()=>setStreaming(true);ws.onclose=()=>setStreaming(false);ws.onmessage=(ev)=>{try{const k=JSON.parse(ev.data)?.k;if(!k)return;const c={t:Number(k.t),o:Number(k.o),h:Number(k.h),l:Number(k.l),c:Number(k.c),v:Number(k.v)};setData(prev=>{const copy=[...prev];const i=copy.findIndex(x=>x.t===c.t);if(i>=0)copy[i]=c;else copy.push(c);return copy.slice(-180)})}catch{}};return()=>ws.close()},[symbol,interval]);
 useEffect(()=>{if(/^[A-Z0-9]{3,20}USDT$/.test(symbol))return;const id=window.setInterval(()=>{void load(symbol)},30000);return()=>window.clearInterval(id)},[symbol,interval]);
 const decision=analysis&&analysis.signal?analysis.signal:"WAIT";
 function choose(r:Result){setSymbol(r.symbol);setQuery(r.symbol);setResults([]);void load(r.symbol)}
 function onFile(e:any){const f=e&&e.target&&e.target.files?e.target.files[0]:null;if(!f)return;setImage(URL.createObjectURL(f))}
 return <div className="app"><header><div className="brand"><b>◉</b><span>SENTINEL<strong> AI</strong></span></div><div className="live">● LIVE GLOBAL MARKETS</div><button className="upload" onClick={()=>fileRef.current&&fileRef.current.click()}>＋ Upload chart</button></header><main>
 <section className="hero"><small>MARKET INTELLIGENCE / VERIFIED DATA</small><h1>See the market.<br/><i>Then make the call.</i></h1><p>Real-time market discovery, multi-factor analysis, chart upload, and news context. No fabricated candles, prices, signals or P&amp;L.</p><div className="search"><span>⌕</span><input value={query} onChange={e=>search(e.target.value)} onKeyDown={e=>{if(e.key==="Enter"&&query.trim())choose({symbol:query.trim().toUpperCase()})}} placeholder="Search BTC, EUR/USD, Apple, Gold, Nasdaq…"/><small>{results.length?"SELECT":"SEARCH"}</small>{results.length>0&&<div className="results">{results.map((r,i)=><button key={r.symbol+i} onClick={()=>choose(r)}><b>{r.symbol}</b><span>{r.name}</span><em>{r.exchange||"GLOBAL"} · {r.type||"MARKET"}</em></button>)}</div>}</div><div className="quick">{quick.map(r=><button key={r.symbol} className={symbol===r.symbol?"sel":""} onClick={()=>choose(r)}>{r.symbol}</button>)}</div></section>
 <section className="market"><div><small>CONNECTED MARKET</small><h2>{symbol}</h2><span>{provider||"Awaiting verified provider"} · ●</span></div><div className="price">{fmt(analysis&&analysis.price)} <small>LIVE</small></div><div className="frames">{["1m","5m","15m","1h","4h","1d"].map(x=><button key={x} className={interval===x?"on":""} onClick={()=>{setInterval(x);load(symbol)}}>{x}</button>)}</div><button className="analyze" onClick={()=>{void load(symbol)}} disabled={loading}>{loading?"ANALYZING…":"ANALYZE ↗"}</button></section>
 <section className="grid"><aside><div className="panel"><small>DISCOVER / LIVE</small>{quick.map(r=><button className={symbol===r.symbol?"row active":"row"} key={r.symbol} onClick={()=>choose(r)}><b>{r.symbol}</b><span>{r.name}</span></button>)}<p className="tiny">Coverage is provider-driven. Search returns instruments exposed by connected public providers; unsupported symbols return NO TRADE.</p></div><div className="panel scanner"><small>CHART SCANNER</small><h3>Upload a chart from your phone.</h3><p>Images stay in this browser. The platform never claims an image was analyzed when no verified vision engine is connected.</p><button onClick={()=>fileRef.current&&fileRef.current.click()}>TAKE PHOTO / CHOOSE IMAGE</button>{image&&<div className="preview"><img src={image} alt="Uploaded trading chart"/><button onClick={()=>{URL.revokeObjectURL(image);setImage("")}}>Remove</button></div>}</div></aside>
 <div className="center"><div className="chartCard"><div className="chartHead"><div><small>PRICE ACTION</small><b>{symbol} · {interval}</b></div><span>{streaming?"● WEBSOCKET STREAM":"● PROVIDER DATA"}</span></div><div className="chart">{data.length>0?<Chart data={data}/>:<div className="empty">{loading?"Connecting to verified market…":"Search a market to load its live chart."}</div>}</div><div className="foot"><span>◉ VERIFIED LIVE DATA</span><span>{data.length} candles</span><span>{provider||"—"}</span></div></div><div className="metrics">{[["EMA 12",analysis&&analysis.emaFast],["EMA 26",analysis&&analysis.emaSlow],["RSI 14",analysis&&analysis.rsi],["ATR 14",analysis&&analysis.atr]].map((m:any)=><div key={m[0]}><small>{m[0]}</small><b>{fmt(m[1])}</b></div>)}</div></div>
 <aside><div className="panel decision"><small>SENTINEL DECISION</small><div className={"signal "+decision.toLowerCase().replace(/ /g,"-")}>{loading?"ANALYZING…":decision}</div><div className="confidence"><b>{analysis&&analysis.confidence!=null?analysis.confidence:"—"}%</b><span>evidence confidence</span></div><div className="decisionMeta"><span>{analysis&&analysis.trend||"NO TREND"}</span><span>{analysis&&analysis.regime||"UNKNOWN"}</span><span>{analysis&&analysis.agreement||0}/5 aligned</span></div><div className="meter"><i style={{width:(analysis&&analysis.confidence?analysis.confidence:0)+"%"}}/></div><div className="reasons"><small>WHY</small>{(analysis&&analysis.reasons?analysis.reasons:["Analyze verified market data to receive evidence."]).slice(0,6).map((x:string,i:number)=><p key={i}>› {x}</p>)}</div><div className="guard"><b>NO GUARANTEE</b><span>Signals are analytical evidence, not certainty and never place trades automatically.</span></div></div></aside></section>
 <section className="news"><div><small>MARKET INTELLIGENCE</small><h2>Latest headlines</h2></div>{news.length>0?news.slice(0,6).map((n,i)=><a key={i} href={n.link} target="_blank" rel="noreferrer">{n.title}<small>{n.pub}</small></a>):<p>News appears after a market is loaded.</p>}</section>{error&&<div className="error">{error}</div>}</main><footer>REAL DATA ONLY · PROVIDER VERIFIED · NO GUARANTEED PROFITS · NO AUTOMATIC EXECUTION</footer><input ref={fileRef} hidden type="file" accept="image/jpeg,image/png,image/webp,image/heic" capture="environment" onChange={onFile}/></div>
}